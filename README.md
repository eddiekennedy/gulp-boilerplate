Super simple gulp boilerplate. Currently all it does it is launch a browser and refresh when changes are made to `index.html` or `app.js`. 

## Usage

- Replace all instances of `XXX`
- Run `npm install`
- Run `gulp`